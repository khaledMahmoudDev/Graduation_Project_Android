<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.graduationproject">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".calender.SingleUser">
        </activity>
        <activity
                android:name=".calender.MainPageCalender"
                android:label="@string/title_activity_main_page_calender"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".calender.Main"
                android:label="@string/title_activity_main">
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key"/>

        <activity
                android:name=".eventLocation.EventLocation"
                android:label="@string/title_activity_event_location">
        </activity>
        <activity android:name=".profile.UpdateProfile">
        </activity>
        <activity android:name=".profile.ForgetPassword">
        </activity>
        <activity android:name=".calender.UpdateThisEvent">
        </activity>
        <activity android:name=".calender.CalendarView">
        </activity>
        <activity android:name=".calender.AddEvent">
            <intent-filter>
                <action android:name="com.gb.action.addEvent"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".calender.Calender">
        </activity>
        <activity android:name=".profile.Profile">
        </activity>
        <activity android:name=".profile.SignIn">
        </activity>
        <activity android:name=".profile.Signup">
        </activity>
        <activity android:name=".location.Location">
        </activity>
        <activity android:name=".weather.Weather">
        </activity>
        <activity android:name=".note.Note">
        </activity>
        <activity android:name=".toDo.ToDo">
        </activity>
        <activity android:name=".email.Email">
        </activity>
        <activity android:name=".mainPac.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>